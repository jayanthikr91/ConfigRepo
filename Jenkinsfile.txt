node{

stage('checkout')

{
checkout scm;
def prop=readProperties  file: 'propertiesfile.txt'
echo "${prop}"
def giturl=prop.GIT_URL
echo "giturl=${giturl}"
git "${giturl}"

}

stage ('build'){

sh 'mvn clean install -Dv=${BUILD_NUMBER}'

}

stage('deploy'){
sh 'sudo cp /var/lib/jenkins/workspace/PipelineDemo/target/Helloworld.war /var/lib/tomcat8/webapps/'
}

}
